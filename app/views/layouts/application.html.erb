<!DOCTYPE html>
<html>
  <head>
    <title>BApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-700 h-screen">
    <% if user_signed_in? %>
      <div class="flex gap-x-2 justify-end items-center pr-2">
        <p class="text-sm text-slate-200">Hi <%= current_user.name %></p>
        <%= link_to "Log out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "self-right text-sm text-slate-200" %>
      </div>
    <% else %>
      <div class="flex gap-x-2 justify-end items-center pr-2">
        <%= link_to "Log in", new_user_session_path, class: "self-right text-sm text-slate-200" %>
        <%= link_to "Sign in", new_user_registration_path, class: "self-right text-sm text-slate-200" %>
      </div>
    <% end %>
    <p class="text-green-600 text-center"><%= notice %></p>
    <p class="text-red-600 text-center"><%= alert %></p>
    <%= yield %>
  </body>
</html>
